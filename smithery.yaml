runtime: "typescript"
env:
  NODE_ENV: "production"
  # Note: The following environment variables should be set in the Smithery dashboard:
  # - OPENROUTER_API_KEY (required)
  # - UPSTASH_VECTOR_REST_URL (required)
  # - UPSTASH_VECTOR_REST_TOKEN (required)
  # - LLM_MODEL (optional, defaults to 'z-ai/glm-4.6')
  # - CHUNK_SIZE (optional, defaults to 1000)
  # - CHUNK_OVERLAP (optional, defaults to 200)
  # - TOP_K (optional, defaults to 5)

